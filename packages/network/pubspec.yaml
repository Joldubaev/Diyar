name: network
description: Unified network layer for Diyar — REST client, error handling, interceptors.
publish_to: none
version: 1.0.0

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  connectivity_plus: ^7.0.0
  dio: ^5.9.0
  equatable: ^2.0.7
  fpdart: ^1.1.0
  logging: ^1.3.0
  storage:
    path: ../storage

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.4
