name: storage
description: Storage package for kg_border monorepo
version: 0.0.1
environment:
  sdk: ">=3.0.0 <4.0.0"
dependencies:
  flutter_secure_storage: ^9.2.4
  shared_preferences: ^2.5.3

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter
